use flake

export DB_DSN=sqlite3://file:$(pwd)/data/db.sqlite?cache=shared
export STORAGE_PATH=$(pwd)"/data"
export HTTP_ADDR="0.0.0.0:8080"
export HOSTNAME=localhost:8080
export FEDERATION_HOST=localhost:8080

mkdir -p $STORAGE_PATH
